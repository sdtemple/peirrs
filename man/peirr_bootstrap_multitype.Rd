% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peirr_bootstrap_multitype.R
\name{peirr_bootstrap_multitype}
\alias{peirr_bootstrap_multitype}
\title{Bootstrap pair-based estimators for multitype epidemic model}
\usage{
peirr_bootstrap_multitype(
  num.bootstrap,
  betas,
  gammas,
  beta.sizes,
  gamma.sizes,
  sample.size,
  p,
  q,
  m = 1,
  e = 0,
  within = 0.1,
  etc = NULL
)
}
\arguments{
\item{num.bootstrap}{Integer: number of bootstrap replicates.}

\item{betas}{Numeric vector: infection rates for each infection type.}

\item{gammas}{Numeric vector: removal rates for each removal type.}

\item{beta.sizes}{Integer vector: population sizes or group sizes corresponding to \code{betas}.}

\item{gamma.sizes}{Integer vector: population sizes or group sizes corresponding to \code{gammas}.}

\item{sample.size}{Numeric: target epidemic size to condition on.}

\item{p}{Numeric: expected proportion of complete pairs observed.}

\item{q}{Numeric: probability that infection time is missing (conditional on missingness).}

\item{m}{Integer: positive shape parameter for infectious period distribution (default 1).}

\item{e}{Numeric: fixed exposure period (default 0).}

\item{within}{Numeric: acceptable proportional deviation from the target sample size (default 0.1).}

\item{etc}{List or NULL: additional arguments passed to \code{peirr_tau_multitype}.}
}
\value{
A numeric matrix of dimension \code{(num.bootstrap + 1) × (length(betas) + length(gammas))}.
The first row contains the original \code{betas} and \code{gammas}, and subsequent rows
contain bootstrap estimates of infection and removal rates.
}
\description{
Perform parametric bootstrapping to estimate infection and removal rates
in a multitype stochastic epidemic model. Each bootstrap replicate simulates
a new epidemic under the given parameters, re-estimates rates using
\code{peirr_tau_multitype}, and stores the results.
}
\details{
Each bootstrap replicate:
\enumerate{
\item Simulates a multitype epidemic using \code{simulator_multitype}.
\item Filters and extracts infection and removal times by type.
\item Re-estimates rates with \code{peirr_tau_multitype}.
}
The resulting matrix can be used to assess estimation uncertainty.
}
\examples{
\dontrun{
results <- peirr_bootstrap_multitype(
  num.bootstrap = 100,
  betas = c(2,2),
  gammas = c(1,2),
  beta.sizes = c(110,90)
  gamma.sizes = c(90,110),
  sample.size = 100,
  p = 0.5,
  q = 0.5
)
}

}
