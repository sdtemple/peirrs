% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_distance_matrix.R
\name{simulate_distance_matrix}
\alias{simulate_distance_matrix}
\title{Simulate a Distance Matrix with Specified Statistical Properties}
\usage{
simulate_distance_matrix(
  population_size,
  kernel = exp,
  inverse_kernel = log,
  mu = 0.9,
  sigma = 0.01,
  method = "euclidean",
  runif_max = 100,
  scalar = -0.05,
  num_tries = 1000
)
}
\arguments{
\item{population_size}{Positive integer. Number of points (observations) for which to
generate distance matrix.}

\item{kernel}{Function. Transformation function to apply to scaled distances.
Must have a corresponding inverse function.}

\item{inverse_kernel}{Function. Inverse of the transformation function \code{kernel}.
Must satisfy \code{inverse_kernel(kernel(x)) ≈ x}.}

\item{mu}{Numeric. Target mean for the transformed distance values.
Default is 0.9.}

\item{sigma}{Numeric. Target standard deviation for the transformed distance
values. Must be positive. Default is 0.01.}

\item{method}{Character. Distance metric to use. Passed to \code{dist()}.
Default is 'euclidean'.}

\item{runif_max}{Numeric. Upper bound for random coordinate generation.
Default is 100.}

\item{scalar}{Numeric. Scaling factor applied to distances before
transformation. Default is -0.05.}

\item{num_tries}{Positive integer. Maximum number of iterations to attempt
finding a valid distance matrix. Default is 1000.}
}
\value{
A numeric matrix of pairwise distances with dimensions N × N,
where the transformed values have mean \code{mu} and standard deviation
\code{sigma}.
}
\description{
Generates a random distance matrix by creating coordinates in 2D space,
computing pairwise distances, applying a transformation function, and
scaling the resulting values to match target mean and standard deviation.
}
\details{
The function iteratively:
\enumerate{
\item Generates N random 2D coordinates
\item Computes pairwise distances using the specified metric
\item Applies transformation h and scales to target mean/sd
\item Stops when all scaled values are non-negative
\item Applies inverse transformation to obtain final distance matrix
}
}
\examples{
\dontrun{
D <- simulate_distance_matrix(population_size=10, kernel=exp, inverse_kernel=log, mu=0.9, sigma=0.01)
}

}
